## Changed

- Added a single module constant `TEST_LABELS = { "paas.test": "ingress" }` to `tests/integration/ingress.itest.ts` and used it for both the `DockerRuntime` labels and the `after` hook's `removeLabelled`, so the file removes only what it created and no longer collides with the contract suite's `paas.test=true` teardown.
- Added `tests/integration-isolation.test.ts`, an offline hygiene test modelled on `tests/contract-hygiene.test.ts`, that reads both itest files with `node:fs` and asserts `ingress.itest.ts` uses `"paas.test": "ingress"` and not `"paas.test": "true"`, while `docker.itest.ts` uses `"paas.test": "true"` and not `"paas.test": "ingress"`.
- Updated the "Ingress test" section of `docs/integration-testing.md`: the ingress test now labels everything `paas.test=ingress` and removes only that, so it can run in parallel with the contract suite, which uses `paas.test=true`. No other part of the file changed.

## Missing context

`verify_starts: red` did not reproduce: before this task, the missing `tests/integration-isolation.test.ts` was silently ignored by `node --test` (v24.21.0), so the verify command exited 0 with only the two itest files skipped. The red state only appears once the hygiene file exists and asserts the new labels, which is the state this task creates.

Touched: tests/integration/ingress.itest.ts, tests/integration-isolation.test.ts, docs/integration-testing.md
