# Tasks

## 1. Admin client

- [x] 1. When the admin client talks to a Caddy that enforces origins, its requests carry no browser headers and succeed

## 2. Integration tier

- [x] 2. When both integration files run in parallel, each removes only the resources it created
