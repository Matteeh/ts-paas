## Changed

- `tests/docs-podman.test.ts` (new): offline hygiene test reading `docs/manual-testing.md` and `README.md` with `node:fs`, like `tests/contract-hygiene.test.ts`. Asserts the manual has no `podman restart`, keeps `docker restart paas-caddy`, and uses `podman kill --signal KILL paas-caddy` with `start-ingress: paas-caddy (started, 1 routes)`; that `## Podman 3.4 port forwarders` sits before `## Recording results` and its section holds `ss -ltnp`, `containers-rootlessport` and `kill <pid>`; and that README's `### Rootless Podman on Linux` subsection mentions `paas reconcile` for a `crashed Caddy` and links to `docs/manual-testing.md#podman-34-port-forwarders`.
- `docs/manual-testing.md`: split step 3 into a Docker path (`docker restart paas-caddy`, reconcile `push-ingress: paas-caddy (1 routes)`) and a rootless Podman path (`podman kill --signal KILL paas-caddy`, `Exited (137)`, reconcile `start-ingress: paas-caddy (started, 1 routes)`), keeping the step's shape and its Caddy-default-page check. Added the `## Podman 3.4 port forwarders` section before `## Recording results`: a `podman`-started container can leave a `containers-rootlessport` process holding its host ports, paas never starts containers that way, and `ss -ltnp | grep :<port>` → `tr '\0' ' ' < /proc/<pid>/cmdline` → `podman ps -a` → `kill <pid>` confirms and fixes it, with a note that WSL 2 relays these ports so a leak also blocks Docker Desktop (`Ports are not available`).
- `README.md`: added one sentence to the rootless Podman subsection — Podman 3.4 does not restart a crashed Caddy, so run `paas reconcile`, with a link to the new port-forwarders section.

## Missing context

None.

Touched: docs/manual-testing.md, README.md, tests/docs-podman.test.ts
