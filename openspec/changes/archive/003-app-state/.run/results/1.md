## Changed

- Added `src/state/paths.ts`: `paasHome` and `stateDbPath` with `PAAS_HOME` precedence.
- Added `src/state/errors.ts`: `StateError` plus the typed store errors, with `new.target.name` naming and the contract messages.
- Added `src/state/db.ts`: `MIGRATIONS` (single v1 schema migration), `openStore` with parent-directory creation, `:memory:` support, `BEGIN IMMEDIATE`/`COMMIT`/`ROLLBACK` per migration, `schema_migrations` bookkeeping, `SchemaTooNewError` refusal, injected clock and id generator, and `schemaVersion`.
- Added `tests/state-db.test.ts` covering every acceptance line, including the spawned silent-open child process.

Touched: src/state/paths.ts, src/state/errors.ts, src/state/db.ts, tests/state-db.test.ts
