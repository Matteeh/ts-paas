## Changed

- Added `src/state/deployments.ts`: `DEPLOYMENT_STATUSES`, `DeploymentStatus`, `FINAL_STATUSES`, `Deployment`, `StatusChange`, and `createDeployment`, `getDeployment`, `setDeploymentStatus`, `latestDeployment`, `deploymentHistory`, `deploymentStatusChanges`. It reuses `getApp`, `validateImage` and the existing store errors, inserts a `deployment_events` row for the initial `pending` and every later change, sets `finishedAt` on entering a final status, refuses changes to finished deployments, and orders latest/history/history-limit and status changes by `seq`.
- Added `tests/state-deployments.test.ts` covering every acceptance line: new deployment shape, unknown app and invalid image, the four-step history with the clock advancing, container id persistence plus `error` and `finishedAt` on failure/stop/replace, final-deployment refusal, unknown-id errors, equal-timestamp creation ordering with `limit`, `null`/empty for an app with no deployments, and the end-to-end `deleteApp` running-block plus cascade.

## Missing context

- Nothing needed beyond the task files and the earlier `src/state` modules.

Touched: src/state/deployments.ts, tests/state-deployments.test.ts
