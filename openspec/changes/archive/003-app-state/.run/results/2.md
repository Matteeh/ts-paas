## Changed

- Added `src/state/validate.ts`: `validateAppName`, `validateImage`, `validatePort`, `validateHostname` (lowercasing) and `validateEnv`, each throwing `ValidationError` with the offending field.
- Added `src/state/apps.ts`: `App`/`NewApp`/`AppChanges`, `createApp`, `getApp`, `listApps` (ordered by name), `updateApp` (partial, whole-env replacement, `hostname: null` clears) and `deleteApp` (blocks on a `running` deployment, otherwise cascades deployments and history).
- Added `tests/state-validate.test.ts` and `tests/state-apps.test.ts` covering every acceptance line, seeding deployments through `store.db` as the task allows.
- SQL is confined to prepared statements in `src/state/apps.ts`; foreign-key cascade removes deployments and events on delete.

## Missing context

- The proposal `pnpm verify` is flaky: `tests/clock.test.ts` "systemClock.sleep waits at least the requested real time" intermittently fails with e.g. "expected at least 5ms, got 4ms" due to real timer resolution. It is outside this task's scope and `tests.modify` is false, so it was left untouched; a re-run passes. Flagging because the watcher runs the proposal verify and could kill the task on that unrelated flake.

Touched: src/state/validate.ts, src/state/apps.ts, tests/state-validate.test.ts, tests/state-apps.test.ts
