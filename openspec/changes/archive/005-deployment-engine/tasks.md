# Tasks

## 1. Health check

- [x] 1. When a new container is started, the health check decides within the window on the injected clock

## 2. Deploy flow

- [x] 2. When an app is deployed, the engine moves the deployment to running or failed and replaces the previous one only after the new one is healthy

## 3. Stop

- [x] 3. When an app is stopped, its running container is stopped and removed and the deployment is marked stopped
