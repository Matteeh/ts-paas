## Changed

- Added `src/deployments/types.ts` with the shared constants (`PAAS_NETWORK`, `APP_LABEL`, `DEPLOYMENT_LABEL`, `DEFAULT_HEALTH_WINDOW_MS`, `DEFAULT_STOP_TIMEOUT_SECONDS`, `HEALTH_LOG_LINES`, `IN_PROGRESS_STATUSES`), `DeployEvent`, `EngineDeps`, and `containerName`.
- Added `src/deployments/errors.ts` with `DeployError` (setting `name = new.target.name`), `DeploymentInProgressError`, and `AppNotRunningError`.
- Added `src/deployments/health.ts` with `checkHealth` and `healthFailureMessage`. `checkHealth` inspects at once, fails without waiting when not running, otherwise sleeps the window on the injected clock and is healthy only when still running with restart count 0 and an unchanged start time; unhealthy results keep the last 20 log texts.
- Added `tests/deploy-health.test.ts` covering every acceptance line, including the pending-until-window assertion, exit timing (1000 ms / exactly 3000 ms / 3001 ms), restart detection through a delegating `ContainerRuntime` wrapper, log tailing, and runtime error propagation.

## Deviated

- None.

## Missing context

- None.

Touched: src/deployments/types.ts, src/deployments/errors.ts, src/deployments/health.ts, tests/deploy-health.test.ts
