## Changed

- Added `src/ingress/config.ts`: `IngressRoute`, `CaddyConfig`, `ADMIN_LISTEN`, the pure `buildCaddyConfig`, `desiredRoutes` (via `listApps` + newest `running` `deploymentHistory` + `containerName`), and the never-throwing `routesFromConfig`.
- Added `src/ingress/admin.ts`: `DEFAULT_ADMIN_URL`, `CaddyAdmin`, `IngressError`, `AdminUnreachableError`, `AdminRequestError` and `HttpCaddyAdmin` (`POST /load`, `GET /config/`), mapping fetch rejections and non-2xx responses to the contracted messages and error names.
- Added `src/ingress/fake-admin.ts`: `FakeCaddyAdmin` with ordered `failures`, awaited `onLoad`, recorded `loads` and last-config `getConfig`.
- Added `tests/ingress-config.test.ts` and `tests/ingress-admin.test.ts` covering every acceptance line, including an in-test `node:http` server on `127.0.0.1:0` and a listened-then-closed refused port.

## Missing context

None.

Touched: src/ingress/config.ts, src/ingress/admin.ts, src/ingress/fake-admin.ts, tests/ingress-config.test.ts, tests/ingress-admin.test.ts
