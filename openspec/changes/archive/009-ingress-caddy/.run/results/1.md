## Changed

- Appended migration 2 to `MIGRATIONS` in `src/state/db.ts`: `CREATE TABLE settings (key TEXT PRIMARY KEY, value TEXT NOT NULL)`.
- Added `src/state/settings.ts` with `TlsMode`, `IngressSettings`, `DEFAULT_INGRESS_SETTINGS`, `getIngressSettings` and `setIngressSettings`. Settings live only in SQL here. Merged values are validated before any write, writes happen in one transaction, and invalid input throws `ValidationError` naming `tls`, `httpPort` or `httpsPort`.
- Added `tests/state-settings.test.ts` covering defaults, partial merge across reopen, TLS mode keeping stored ports, invalid values storing nothing, and equal merged ports naming `httpsPort`.

## Deviated

None.

## Missing context

None.

Touched: src/state/db.ts, src/state/settings.ts, tests/state-settings.test.ts
