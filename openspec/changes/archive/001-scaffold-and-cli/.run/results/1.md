## Changed

- `package.json`: set `version` to `0.0.0`, added `type: "module"`, `engines.node: ">=24"`, and `bin.paas: "dist/cli.js"`; removed `main`. Replaced the placeholder `test`/`verify` scripts with `test` (`node --import tsx --test "tests/**/*.test.ts"`), `verify` (`tsc -p tsconfig.json && pnpm test`), and added `build` (`tsc -p tsconfig.build.json`). `devEngines`, `packageManager`, and all dependency versions left untouched.
- `tsconfig.json`: strict, `module`/`moduleResolution` `nodenext`, `target` `es2024`, `types` `["node"]`, `noEmit`, `verbatimModuleSyntax`, `skipLibCheck`, including `src` and `tests`.
- `tsconfig.build.json`: extends the base config, enables emit with `rootDir: "src"` and `outDir: "dist"`, includes only `src`.
- `src/version.ts`: exports `packageVersion()`, which reads and parses `../package.json` resolved against `import.meta.url` (works from both `src/` and `dist/`).
- `tests/version.test.ts`: new test asserting `packageVersion()` equals the `version` read from `package.json` on disk.

## Missing context

- None. The task body and delta spec fully specified the required config and behavior.

## Next

- None.

Touched: package.json, tsconfig.json, tsconfig.build.json, src/version.ts, tests/version.test.ts
