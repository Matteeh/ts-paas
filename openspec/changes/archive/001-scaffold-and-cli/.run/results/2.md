## Changed

- `src/output.ts`: added the `Io` interface, `processIo` (writing to `process.stdout`/`process.stderr`), and `writeOutput`, which writes one JSON line in JSON mode and the formatter output followed by a newline in human mode.
- `src/errors.ts`: added `UsageError`.
- `src/program.ts`: added `buildProgram(io?)` (fresh commander program named `paas` with `--version`/`-V` bound to `packageVersion()`, `exitOverride()`, `showHelpAfterError()`, `configureOutput()` routing through `Io` and rewriting the leading `error: ` to `paas: `, and a root action that prints help) and `run(argv, io?, program?)` returning 0 for success and commander exit-code-0 outcomes, 2 for usage errors (commander non-zero errors and thrown `UsageError`, the latter also printing the usage text), and 1 for any other thrown error.
- `src/cli.ts`: shebang entry point assigning `process.exitCode = await run(process.argv.slice(2))`.
- `tests/output.test.ts`: new tests for `writeOutput` JSON and human modes.
- `tests/cli.test.ts`: new in-process tests for no-args/`--help`/`-h`, command descriptions, `--version`/`-V`, unknown command, unknown option, thrown `UsageError`, thrown `Error`, the `cli.ts` shebang/exit-code shape, and a `tsx` child-process test.

## Missing context

- None. The task body specified the ports, commander mechanisms, and behavior.

## Next

- None.

Touched: src/cli.ts, src/program.ts, src/errors.ts, src/output.ts, tests/cli.test.ts, tests/output.test.ts
