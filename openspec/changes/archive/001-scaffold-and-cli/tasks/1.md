---
title: When pnpm verify runs, it typechecks the project and runs every unit test
verify: pnpm verify
verify_starts: red
scope: [package.json, tsconfig.json, tsconfig.build.json, src/version.ts, tests/version.test.ts]
entry: [package.json]
tests:
  modify: false
skills: []
---
## Context

Dependencies are already installed by a human: `commander`, `typescript` (7.x, provides `tsc`), `tsx`, and `@types/node`. Do not add, remove, or change any dependency, and do not touch `pnpm-lock.yaml` or `devEngines`/`packageManager`. There is no network.

TypeScript 7 does not load `@types/*` automatically; `types` must list `node`. Relative imports use the `.js` extension (`./version.js`); `tsx` resolves them to `.ts`.

## Acceptance
- [ ] `package.json` has `name` `ts-paas`, `version` `0.0.0`, `type` `module`, `engines.node` `>=24`, `bin` `{ "paas": "dist/cli.js" }`, and no `main`.
- [ ] Scripts: `test` is `node --import tsx --test "tests/**/*.test.ts"`, `verify` is `tsc -p tsconfig.json && pnpm test`, `build` is `tsc -p tsconfig.build.json`.
- [ ] `tsconfig.json` is strict with `module` and `moduleResolution` `nodenext`, `target` `es2024`, `types` `["node"]`, `noEmit`, `verbatimModuleSyntax`, `skipLibCheck`, and includes `src` and `tests`.
- [ ] `tsconfig.build.json` extends `tsconfig.json`, turns emit on with `rootDir` `src` and `outDir` `dist`, and includes only `src`.
- [ ] `src/version.ts` exports `packageVersion()`, which reads `version` from `../package.json` resolved against `import.meta.url`, so it works from both `src/` and `dist/`.
- [ ] `tests/version.test.ts` asserts `packageVersion()` equals the `version` in `package.json` read from disk.
- [ ] `pnpm verify` exits 0.
