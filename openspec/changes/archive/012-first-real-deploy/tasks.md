# Tasks

## 1. Reconcile

- [x] 1. When reconcile runs with --redeploy, it brings back every app whose running deployment reconcile failed, in this run or an earlier one

## 2. paas up

- [x] 2. When a user runs paas up, it checks the engine, ensures paas-net, brings ingress up and prints the next command

## 3. Release

- [x] 3. When PAAS_E2E=1, pnpm test:e2e runs the first real deploy end to end, and the package ships as 0.1.0 with a quickstart
