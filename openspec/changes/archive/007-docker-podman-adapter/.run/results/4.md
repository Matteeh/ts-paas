## Changed

- `src/commands/context.ts`: dropped `NO_RUNTIME_MESSAGE` and the throwing placeholder. `defaultRuntimeFactory(env)` returns `new DockerRuntime({ socketPath: resolveSocket(env).path })`; `getRuntime(context)` returns `context.runtime?.() ?? defaultRuntimeFactory(context.env)`; `withEngine` now resolves the runtime, `await runtime.ping()` before `withStore`, so an unreachable engine leaves `state.db` untouched.
- `src/commands/doctor.ts` (new): `registerDoctor` adds `doctor` (`check the container engine`, `--json`). It calls `getRuntime`, pings, calls `networkExists(PAAS_NETWORK)` (never `ensureNetwork`), never opens the store, and prints the four label-padded lines via `writeOutput`. `--json` emits `{socket, engine, version, apiVersion, paasNet}`.
- `src/program.ts`: registers `registerDoctor` after the existing commands.
- `tests/commands-runtime.test.ts`: the three default-factory/`NO_RUNTIME_MESSAGE` tests now cover the existing-socket `DockerRuntime`, the missing-socket `RuntimeUnavailableError` message, the missing-`PAAS_SOCKET` `withEngine` failure, and the injected-`unavailable` fake failing on ping before the store exists. Every other test is unchanged.
- `tests/deploy-cli.test.ts`: only the child-process test changed; it now sets a missing `PAAS_SOCKET`, asserts stderr exactly `paas: cannot reach container engine at <path>: socket not found\n`, exit 1, and `status` still exits 0 with `-`.
- `tests/doctor-cli.test.ts` (new): fake report and exit 0, `paas-net: present` after `ensureNetwork`, the exact `--json` line, a fake reporting `apiVersion`/`endpoint` without `ensureNetwork`, the child-process doctor failure with no `state.db`, help listing, and a walk of `src/` asserting no `NO_RUNTIME_MESSAGE` or old message.

## Deviated

- None.

## Missing context

- None.

Touched: src/commands/context.ts, src/commands/doctor.ts, src/program.ts, tests/commands-runtime.test.ts, tests/deploy-cli.test.ts, tests/doctor-cli.test.ts
