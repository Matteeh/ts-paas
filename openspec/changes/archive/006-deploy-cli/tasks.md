# Tasks

## 1. Runtime context

- [x] 1. When a command needs the runtime, the clock or a duration, it gets them from the command context and shared helpers

## 2. Deploy commands

- [x] 2. When a user runs paas deploy, status, logs or stop, the engine runs with printed progress and state is shown per the contract
