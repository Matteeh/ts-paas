## Changed

- `src/commands/context.ts`: added optional `runtime?: () => ContainerRuntime` and `clock?: Clock` to `CommandContext`; added `NO_RUNTIME_MESSAGE`, `defaultRuntimeFactory`, `contextClock`; `withStore` now opens the store with `{ clock: contextClock(context) }`; added `withEngine`, which resolves the runtime factory before opening the store and passes `{ store, runtime, clock }` (no `onProgress`).
- `src/commands/deploy-helpers.ts`: new module with `parseDuration`, `parseTail`, `formatUptime` and `shortContainerId` as contracted.
- `tests/commands-runtime.test.ts`: new test file covering every acceptance line (clock-aware `withStore`, `withEngine` deps/closure/no-runtime ordering, the `run`+`runAction` probe, and the four helpers).

Touched: src/commands/context.ts, src/commands/deploy-helpers.ts, tests/commands-runtime.test.ts
