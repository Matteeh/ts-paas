## Changed

- Added `tests/contract-hygiene.test.ts`. It reads the contract and deploy
  test sources and asserts the contract suite no longer extends `Object`
  globally and each of the three deploy `DelegatingRuntime` classes contains
  `networkExists(`; it checks `engineLabel` for an endpoint-bearing engine and
  for one without, and that the integration file pings and labels with
  `engineLabel`.
- `tests/support/runtime-contract.ts`: removed the `declare global {
  interface Object { ... } }` block and its comment, and added the exported
  `engineLabel(info)` returning `` `${info.name} (${info.endpoint ?? "unknown
  endpoint"})` ``.
- `tests/deploy-engine.test.ts`, `tests/deploy-health.test.ts`,
  `tests/deploy-stop.test.ts`: each `DelegatingRuntime` now has
  `networkExists(name)` delegating to `this.inner.networkExists(name)`. No
  other change.
- `tests/integration/docker.itest.ts`: in the `PAAS_INTEGRATION=1` branch,
  ping once with `const info = await runtime.ping()` and register the contract
  suite with `engineLabel(info)` instead of the hard-coded `docker
  (${socketPath})`. The skip branch is unchanged.

## Missing context

Nothing blocking. `src/runtime/docker.ts`, `tests/docker-runtime.test.ts` and
`tests/docker-engine-fixes.test.ts` show as modified but belong to task 1 and
were not touched here.

## Next

All acceptance lines pass. Nothing left for this task.

Touched: tests/contract-hygiene.test.ts, tests/support/runtime-contract.ts, tests/deploy-engine.test.ts, tests/deploy-health.test.ts, tests/deploy-stop.test.ts, tests/integration/docker.itest.ts
